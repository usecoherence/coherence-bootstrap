/target/
# Local cursor_agent_coredb_loop.sh evidence (timestamped rounds)
/.agent-runs/
/.coherence/run/
# Managed per-run evidence (ADR-0005); local / disposable — do not commit large artifacts
/.coherence/runs/
/.coherence/review/
/.dolt/run/
.DS_Store
*.log
__pycache__/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
config.yaml

# bv (beads viewer) local config and caches
.bv/
